<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="Budgie.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:Budgie"
    Shell.FlyoutBehavior="Disabled">

    <TabBar>
        <ShellContent
            Title="Create"
            ContentTemplate="{DataTemplate local:CreateBudgetPage}"
            Route="CreateBudgetPage" />

        <ShellContent
            Title="Home"
            ContentTemplate="{DataTemplate local:HomePage}"
            Route="HomePage"/>

        <ShellContent
            Title="View"
            ContentTemplate="{DataTemplate local:ViewBudgetPage}"
            Route="ViewBudgetPage"/>
    </TabBar>

</Shell>

